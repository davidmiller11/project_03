/*
var ScoreView = Backbone.View.extend({
  
  // DOM element for a score on the leaderboard is a 'table row' tag
  tagName: "tr", 

  // Cache the template function for a single score
  template: _.template($('#score-template').html()),

  // Renders the score onto the page
  render: function() {
    this.$el.html(this.template(this.model.toJSON()));
    return this;
  }
  
});
*/